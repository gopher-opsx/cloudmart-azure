# CloudMart Azure Terraform

The finalized lessons expect a separate remote-state bootstrap plus the `environments/training` workload root. Populated `terraform.tfvars` and `.tfbackend` files stay untracked; commit only examples.

Before recording any Terraform lesson, run `terraform fmt -check`, `terraform init`, `terraform validate`, and a reviewed `terraform plan` against the real Azure subscription.
