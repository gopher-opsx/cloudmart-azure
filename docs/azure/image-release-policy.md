# Image Release Policy

CloudMart publishes a human-readable release tag and `sha-<12-char-commit>` tag. Deployment must ultimately identify the immutable registry digest. The full Git revision is stored as OCI metadata.
